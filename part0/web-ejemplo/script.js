const list = [];
const anotherList = list.concat(157);

const persona = {
    name: 'Nelson',
    twitter: '@nelsondiaze',
    age: 55,
    isDeveloper: true,
    links: ['https://nelsondiaze.tube', 'https://nelsondiaze.live']
}

// Expresion
const suma = (operando1, operando2) => {
    console.log(operando1);
    console.log(operando2);
    return operando1 + operando2;
}

//Declaracion
 function restar(a, b){
    return a - b;
 }

const op1 = 8;
const op2 = 2;

console.log(suma(op1, op2))